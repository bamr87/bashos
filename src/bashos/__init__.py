"""bashOS — a terminal-first AI runtime.

The terminal is the core access point. Commands are markdown specs shared with
Claude Code; a LangGraph kernel routes them through orchestration loops; the
model runtime rides on Claude Code OAuth.
"""

__version__ = "0.1.0"
